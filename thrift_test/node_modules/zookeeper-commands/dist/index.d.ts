export * from './client';
export declare class Zookeeper {
    private options;
    constructor(options?: IOptions);
    command(command: string, callback?: (err, data) => void): Promise<any>;
}
export interface IOptions {
    host: string;
    port: number;
    timeout_ms: number;
}
export declare class Options implements IOptions {
    host: string;
    port: number;
    timeout_ms: number;
    constructor(host?: string, port?: number, timeout_ms?: number);
}
